<div class="ps-5 pe-5">
    <div style="display:flex;justify-content:center">
        <div class="w-50 alert alert-info p-4 mt-5">
            <h1 style="display:flex;justify-content:space-between">Chỉnh sửa
            <% if authenticated? %><%= button_to "Xóa", @product, method: :delete, data: { turbo_confirm: "Are you sure?" }, class:"btn btn-danger" %><% end %>
            </h1>
            <%= render "form", product: @product %>
        </div>
    </div>
</div>
